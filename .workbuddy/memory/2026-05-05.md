# 2026-05-05

## AI Claw - AiView 重构完成

将 `projecthub/src/views/ai/AiView.vue`（1175行单文件）重构为 `ai-refactor/` 目录下的组件化架构：

### 目录结构
```
ai-refactor/
├── AiView.vue                  ← 主视图（组装层，~100行）
├── composables/
│   ├── useBalance.js           ← 余额查询与轮询
│   ├── useConversations.js     ← 对话列表CRUD
│   ├── useMessages.js          ← 消息流/流式渲染/draft操作
│   └── useTerminal.js          ← 终端命令检测与执行
└── components/
    ├── ConvSidebar.vue         ← 左侧对话列表+搜索
    ├── ChatTopbar.vue          ← 顶栏（标题/模型选择/余额）
    ├── ChatWelcome.vue         ← 欢迎屏+快捷提示
    ├── MessageList.vue         ← 消息列表容器
    ├── MessageBubble.vue       ← 单条消息气泡（含附件/思考/draft）
    ├── ActionDraftCard.vue     ← 操作草案卡片
    ├── TerminalResultCard.vue  ← 终端执行结果卡片
    └── ChatInputArea.vue       ← 输入区（附件+文本框+发送）
```

### 关键设计决策
- `checkAndExecuteTerminalCommand` 循环依赖问题：通过 sendMessage 运行时参数注入解决（`runtimeCheck ?? constructorCheck`）
- `messagesContainer` DOM ref：通过 MessageList 组件 `defineExpose({containerRef})` 暴露，主视图用 `computed(() => messageListRef.value?.containerRef)` 获取
- 路由已正确指向 ai-refactor/AiView.vue，无需修改
