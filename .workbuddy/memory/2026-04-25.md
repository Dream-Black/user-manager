# 2026-04-25 工作记录

## 桌面端架构设计（已修订）

- 完成 AI Claw / ProjectHub 桌面端适配架构设计
- 最终方案：Electron 纯壳层 + IPC Bridge 转发给本地 Python Proxy(:6789)
- 核心原因：公网 HTTPS 页面无法请求 HTTP localhost（浏览器 Mixed Content / CORS），导致 Python Proxy 的全部本地资源功能在公网部署时失效
- Python Proxy 接口已确认：health/system/info/files/list/files/tree/files/read/comics/scan/resource-paths/test/config/paths
- Python 保留且未来更重要（AI工作流+本地模型推理能力，Node.js 无法替代）
- 三阶段计划：Phase1 本地构建+IPC Bridge跑通 → Phase2 自动更新（基于 version.json）→ Phase3 CI/CD（新增 desktop.yml，v*.*.* tag 触发，deploy.yml 不改）
- 更新策略：启动后延迟检查版本，引导到 GitHub Releases 手动下载
- 新增 Secret：DESKTOP_SERVER_URL；现有三个 Secret 不动
- ADR 已写入 ai/design/ADR-001-desktop-adaptation.md
- 可执行提示词方案已写入 ai/design/Desktop-Execution-Plan.md（供新对话使用）
